# Source Asset Packages

This area preserves original/cleaned source packages and provenance references. Source packages are archival/staging material and are not imported automatically by application code.

Current supplied source packages:

- `ASSET_GAME_MASTER_2026-08-25_CLEANED.zip`
- `Asset-library-LPC_Finalized_Review.zip`

The binary packages are retained in the project upload/checkpoint until a binary-capable repository transfer is performed. Do not claim that individual binaries are present in GitHub until that transfer is verified.