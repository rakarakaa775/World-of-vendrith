import type { MapDocument, MapObject } from './map-document';
export type Entrance={id:string;buildingId:string;side:'north'|'south'|'east'|'west';x:number;y:number;destinationMapId:string|null};
export function getBuildingEntrances(document:MapDocument,buildingId:string):Entrance[]{const b=document.layers.flatMap(l=>l.objects).find(o=>o.id===buildingId);if(!b||b.kind!=='building')return [];return[{id:`${buildingId}-south`,buildingId,side:'south',x:b.x+Math.floor(b.width/2),y:b.y+b.height,destinationMapId:null}]}
export function linkEntrance(document:MapDocument,buildingId:string,entranceId:string,destinationMapId:string):MapDocument{const entrances=getBuildingEntrances(document,buildingId);if(!entrances.some(e=>e.id===entranceId)||!document.layers.some(l=>l.objects.some(o=>o.id===buildingId)))return document;const markerId=`entrance:${entranceId}`;return{...document,layers:document.layers.map(l=>l.id==='objects'?{...l,objects:l.objects.map(o=>o.id===buildingId?{...o,entranceId:markerId,destinationMapId}:o)}:l)}}
