# GITHUB UPDATE NOTE

This checkpoint is intended as an initial/recovery baseline.

Asset packages remain external. Database source must not be inferred from migration names alone; only exact verified SQL should be added to source control.

Future database changes should be captured as additive migrations from a verified baseline.
